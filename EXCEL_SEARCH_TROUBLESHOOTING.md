# 엑셀 검색 문제 해결 가이드

## 문제 상황
엑셀 파일에 "TOMMS 프로젝트 테스트과제" 정보가 있지만, 챗봇이 "문서에서 해당 정보를 찾을 수 없습니다"라고 답변

---

## 🔍 원인 분석

### 가능한 원인 3가지

1. **엑셀 파일이 제대로 처리되지 않음**
   - 업로드는 되었지만 텍스트 추출 실패
   - 특수 문자나 병합된 셀로 인한 오류

2. **키워드 검색 실패**
   - "TOMMS"와 실제 엑셀 내용의 표기가 다름 (예: TOMM, TOMMS, tomms)
   - 벡터 검색 알고리즘이 정확한 매칭을 못함

3. **벡터 DB에 저장되지 않음**
   - 업로드 후 처리 중 오류 발생
   - 처리 완료 표시되었지만 실제로는 저장 안 됨

---

## ✅ 즉시 해결 방법

### 방법 1: 파일 재업로드 (권장) 🔄

1. **관리자 모드** → **자료 업로드** 탭
2. 기존 파일 **삭제**
3. 엑셀 파일 **다시 업로드**
4. "처리 완료" 상태 확인
5. **1~2분 대기** (벡터 DB 인덱싱 시간)
6. 다시 질문

### 방법 2: 질문 방식 변경 🗣️

정확한 키워드를 포함하여 질문하세요:

❌ 나쁜 질문:
```
"테스트과제에 대해 설명해줘"
"프로젝트 정보 알려줘"
```

✅ 좋은 질문:
```
"TOMMS 프로젝트에 대해 설명해줘"
"TOMMS 테스트과제의 목적은?"
"TOMMS 관련 모든 정보를 보여줘"
```

### 방법 3: 엑셀 파일 확인 📊

엑셀 파일에서 확인할 사항:
- ✅ "TOMMS" 키워드가 정확히 들어있는가?
- ✅ 병합된 셀이 많지 않은가?
- ✅ 특수 문자나 이미지가 많지 않은가?
- ✅ 시트 이름이 명확한가?

---

## 🔧 고급 해결 방법

### 1️⃣ 엑셀 검색 테스트 스크립트 실행

```bash
cd C:\AI chat\backend
.\venv\Scripts\Activate.ps1
python test_excel_search.py
```

**입력 사항:**
1. 엑셀 파일 경로 (예: `C:\Users\...\your_file.xlsx`)
2. 검색 키워드 (예: `TOMMS`)

**테스트 결과 확인:**
- ✅ 키워드가 텍스트에서 발견되는가?
- ✅ RAG 검색 결과가 나오는가?
- ✅ 최종 답변이 생성되는가?

---

### 2️⃣ 엑셀 데이터 분석 API 사용

업로드된 파일 ID 확인 후:

```bash
# PowerShell에서 실행
$fileId = 1  # 업로드된 파일 ID
$keyword = "TOMMS"

# 엑셀 파일 분석
Invoke-RestMethod -Uri "http://localhost:8000/api/upload/analyze-excel/$fileId" -Method GET

# 엑셀 내 검색
Invoke-RestMethod -Uri "http://localhost:8000/api/upload/search-excel/${fileId}?keyword=$keyword" -Method POST
```

---

### 3️⃣ 엑셀 파일 구조 개선

**현재 엑셀 구조:**
```
| 과제명 | 설명 | 담당자 |
| TOMMS | ... | ... |
```

**개선된 엑셀 구조:**
```
| 과제명 | 과제 설명 | 담당자 | 키워드 |
| TOMMS 프로젝트 테스트과제 | 상세 설명... | 홍길동 | TOMMS, 테스트, 프로젝트 |
```

**개선 포인트:**
1. 과제명에 전체 이름 포함
2. 별도 키워드 열 추가
3. 시트명을 명확하게 (예: "TOMMS_과제정보")

---

## 💡 검색 품질 향상 팁

### 1. 엑셀 파일 업로드 시

- ✅ 파일명을 명확하게 (예: `TOMMS_프로젝트_정보.xlsx`)
- ✅ 시트명을 의미있게 (예: "과제목록", "TOMMS정보")
- ✅ 첫 번째 행을 헤더로 사용
- ✅ 빈 행이나 병합 셀 최소화

### 2. 질문할 때

- ✅ 엑셀에 있는 **정확한 키워드** 사용
- ✅ 구체적으로 질문 (예: "TOMMS 프로젝트의 목적은?")
- ✅ 여러 방식으로 질문 시도

### 3. 업로드 후

- ✅ 처리 완료 확인
- ✅ 1~2분 대기 후 질문
- ✅ 문서 목록에서 파일 확인

---

## 🎯 빠른 해결 체크리스트

- [ ] 엑셀 파일이 업로드되었나?
- [ ] 처리 상태가 "완료"인가?
- [ ] 엑셀에 "TOMMS" 키워드가 정확히 있나?
- [ ] 질문에 "TOMMS" 키워드를 포함했나?
- [ ] 파일 업로드 후 1~2분 대기했나?
- [ ] 다른 문서는 검색되나?

---

## 📞 여전히 안 될 때

### 1. 로그 확인

백엔드 로그 확인:
```powershell
Receive-Job -Name "Backend" -Keep | Select-Object -Last 50
```

### 2. ChromaDB 초기화

데이터가 꼬인 경우:
```bash
cd C:\AI chat\backend
Remove-Item -Recurse -Force chroma_db
# 그 다음 서버 재시작 후 파일 재업로드
```

### 3. 수동 테스트

```python
# Python 콘솔에서
from app.services.file_processor import FileProcessor

text = FileProcessor.extract_text("your_file.xlsx")
print("TOMMS" in text)  # True가 나와야 함
print(text)  # 전체 내용 확인
```

---

## 📊 예제: TOMMS 프로젝트 샘플 엑셀

샘플 엑셀 구조 (참고용):

**시트명: TOMMS_과제정보**

| 과제명 | 과제 코드 | 설명 | 목적 | 기간 | 담당자 | 예산(원) |
|--------|----------|------|------|------|--------|----------|
| TOMMS 프로젝트 테스트과제 | TOMMS-2024-T01 | 테스트 자동화 시스템 구축 | 품질 향상 | 2024.01~2024.12 | 김철수 | 300,000,000 |

이렇게 구조화하면 챗봇이 쉽게 찾을 수 있습니다!

---

## ✅ 권장 해결 순서

1. **파일 재업로드** (가장 간단)
2. **질문 방식 변경** (키워드 명확히)
3. **test_excel_search.py 실행** (문제 진단)
4. **엑셀 구조 개선** (근본 해결)

---

**대부분의 경우 1번(재업로드)과 2번(질문 변경)으로 해결됩니다!** 🎉

